package Composites;

import java.awt.Color;

public class Form extends BaseControlComposite {
    public Form(String name, int width, int height, Color foreColor, Color backColor) {
        super(name, width, height, foreColor, backColor);
    }
}