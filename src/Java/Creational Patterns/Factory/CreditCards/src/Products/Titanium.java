package Products;

import java.math.BigDecimal;

public class Titanium extends CreditCard {
    public Titanium(String model, BigDecimal limit, BigDecimal annualCharge) {
        super(model, limit, annualCharge);
    }
}
